<section class="featured-events">
  {{#each featuredEvents}}
  <div class="featured-event">
    <h2>
      <a href="event/id">{{name}}</a>
      <p class="">{{description}}</p>
      <p class="date">{{date}}</p>
    </h2>
  </div>
  {{/each}}
</section>  
